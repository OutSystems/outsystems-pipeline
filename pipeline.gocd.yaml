---
format_version: 4
pipelines:
  preflight-demo:
    group: www
    label_template: "${git[:8]}"
    materials:
      git: https://github.com/OutSystems/outsystems-pipeline
      branch: github_actions
    stages:
      - test:
          jobs:
            test:
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - echo hello